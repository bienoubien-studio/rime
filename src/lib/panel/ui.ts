// For shared ui components ex: use a panel button in a plugin
// let's wait a bit before total implementation :)
import Button from './components/ui/button/button.svelte';
import SpinLoader from './components/ui/spin-loader/SpinLoader.svelte';

export { Button, SpinLoader };
