/**
 * Browser-safe config import
 * This file serves as the entry point for importing the browser-safe config
 */

// Import the browser-safe config from our virtual module
import browserConfig from 'virtual:rizom-config';

// Export the browser config for use in client-side components
export default browserConfig;
